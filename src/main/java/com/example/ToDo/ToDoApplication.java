package com.example.ToDo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ToDoApplication {

	public static void main(String[] args) {
		SpringApplication.run(ToDoApplication.class, args);
	}
	
	// dovrsi ostale ostale rute


	// NOVI PROJEKAt
	// napravi projekat koji ima sledece entitete: User(id, firstName, lastName, email, password)
	// Ingredient(id, name)
	// Recipe(id, name, description, user)


	// WeightedIngredient(id, value, unit, ingredient, recipe)


	// dto
	// repoziturijume76

	// servise

	//kontrolere



	//  /ingredients - POST



	// /recipes - POST (name, opis, weightedingredients)


	// /recipes/<id>/weightedingredients - POST


	// /recipes/<id>/weightedingredients - GET











}
